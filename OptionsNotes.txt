Functional tests
=======================
- Parse positional arguments
- Parse errors
	- Value cannot be parsed (e.g. 'aaa' as number)
	- Option is specified without a value, the parser should not swallow the next value (?)
	- Option specified multiple times (e.g. once with short name and once with long name)
- Mixture of all

Custom parsers
=======================
- Factoring - later using DI container etc.
- Internal parsers -> predefined instances

- OptionsTypeDescriptor
	- TryFind positional and named should return PropertyInfo and IValueParser

- Descriptor returns the parser type
	- Either property has an attribute
	- Or return something from the default parsers (rename the class)

- IValueParserFactory
	- CanCreateParser(Type type): bool -> Use when validating options
	- CreateParser(Type type): IValueParser -> Use when parsing options


- IValueParser, IValueParser<TResult> -> wrap the custom parser in internal parser
-> Validate the generic type against the type of the property